import type { NextConfig } from "next";

// Phase A3 - JS/TS 설정 동기화
// 1. next.config.js와 next.config.ts가 동시 존재 (JS 파일은 나중에 제거 예정)
// 2. 현재 두 파일의 설정 내용은 동일함 (기본 설정만 존재)
// 3. 향후 설정 변경 시 TS 파일을 기준으로 동기화 필요

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
